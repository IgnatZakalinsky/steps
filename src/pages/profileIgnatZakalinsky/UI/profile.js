import React from 'react';
import ProfileContainer from "./profileContainer";

const Profile = () => {
    return (
        <div>
            Profile
            <ProfileContainer/>
        </div>
    );
};

export default Profile;
